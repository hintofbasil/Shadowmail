echo "user = $DB_USERNAME" >> /etc/postfix/mysql-aliases.cf
echo "password = $DB_PASSWORD" >> /etc/postfix/mysql-aliases.cf
echo "hosts = $DB_HOSTS" >> /etc/postfix/mysql-aliases.cf

echo "mail.$MAILNAME	OK" >> /etc/postfix/virtual_mailbox_domains
postmap /etc/postfix/virtual_mailbox_domains
